# .agents - template_data_descriptor

Project-local agent scaffolding for the `template_data_descriptor` exemplar.
The project skill lives under [`skills/template-data-descriptor/`](skills/template-data-descriptor/README.md);
project behavior remains governed by the root and nearest `AGENTS.md` files.
